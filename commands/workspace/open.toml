description = "Launch an isolated workspace for a specific Pull Request."
prompt = """
I am preparing your isolated workspace for PR #{{args}}.
This will:
1. Fetch the latest changes from the repository.
2. Spin up a dedicated container.
3. Drop you into a persistent tmux session.

!{npx tsx scripts/orchestrator.ts {{args}} }
"""
