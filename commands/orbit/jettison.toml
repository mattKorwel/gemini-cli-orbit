description = "Decommission a specific mission Orbit capsule and its worktree."
prompt = """
I am decommissioning the capsule for {{args}}.

!{ node "$(ls -d ~/.gemini/extensions/orbit 2>/dev/null || echo .)/bundle/jettison.js" {{args}} }
"""
