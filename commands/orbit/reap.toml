description = "Automated resource cleanup: identify and jettison idle mission capsules."
prompt = """
Starting resource reaping...

This will:
1. Scan all active mission capsules for idle time.
2. Identify capsules with no terminal activity for more than the threshold (default 4h).
3. Offer to jettison idle missions and potentially splashdown the host station.

!{node bundle/reap.js {{args}} }
"""
