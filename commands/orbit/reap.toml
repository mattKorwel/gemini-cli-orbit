description = "Perform orbital cleanup of idle or completed missions."
prompt = """
Reaping stale capsules...

!{ node "$(ls -d ~/.gemini/extensions/orbit 2>/dev/null || echo .)/bundle/reap.js" {{args}} }
"""
