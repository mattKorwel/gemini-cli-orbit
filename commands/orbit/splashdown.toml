description = "Full mission splashdown: destroy the station, all capsules, and the remote disk."
prompt = """
⚠️ DANGER: You are about to initiate a FULL MISSION SPLASHDOWN.

This will:
1. Terminate the GCE station instance.
2. Permanently delete the persistent data disk.
3. Remove all constellation-wide settings.

!{npx tsx scripts/splashdown.ts --all }
"""
