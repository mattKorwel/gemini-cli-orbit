description = "Emergency shutdown of all active remote capsules and the station."
prompt = """
Executing emergency splashdown...

!{ node "$(ls -d ~/.gemini/extensions/orbit 2>/dev/null || echo .)/bundle/splashdown.js" --all }
"""
