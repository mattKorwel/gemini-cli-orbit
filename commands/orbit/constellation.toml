description = "Manage the entire mission constellation of stations across projects and zones."
prompt = """
Managing constellation...

!{ node "$(ls -d ~/.gemini/extensions/orbit 2>/dev/null || echo .)/bundle/fleet.js" {{args}} }
"""
