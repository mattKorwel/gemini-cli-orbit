description = "Retrieve detailed mission telemetry and history logs for an identifier."
prompt = """
Retrieving blackbox logs for {{args}}...

!{ node "$(ls -d ~/.gemini/extensions/orbit 2>/dev/null || echo .)/bundle/blackbox.js" {{args}} }
"""
