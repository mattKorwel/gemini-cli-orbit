description = "Quickly connect to an existing mission session (attach to tmux)."
prompt = """
Establishing uplink to {{args}}...

!{ node "$(ls -d ~/.gemini/extensions/orbit 2>/dev/null || echo .)/bundle/uplink.js" {{args}} }
"""
