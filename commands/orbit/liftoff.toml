description = "Initial station setup: provision GCE Worker and Docker base."
prompt = """
Setting up Orbit station...

!{ node "$(ls -d ~/.gemini/extensions/orbit 2>/dev/null || echo .)/bundle/setup.js" {{args}} }
"""
