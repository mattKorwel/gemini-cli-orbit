description = "Initial station liftoff: provision the GCE Worker and Docker base."
prompt = """
Starting station liftoff...

This will:
1. Create the GCE Worker instance.
2. Configure the persistent data disk.
3. Pull the base 'capsule' image.

!{npx tsx scripts/setup.ts {{args}} }
"""
